import { DashboardCards.Enum } from './dashboard-cards.enum';

describe('DashboardCards.Enum', () => {
  it('should create an instance', () => {
    expect(new DashboardCards.Enum()).toBeTruthy();
  });
});
