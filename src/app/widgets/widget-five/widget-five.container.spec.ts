import { WidgetFive.Container } from './widget-five.container';

describe('WidgetFive.Container', () => {
  it('should create an instance', () => {
    expect(new WidgetFive.Container()).toBeTruthy();
  });
});
