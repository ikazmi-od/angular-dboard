import { WidgetOne.Container } from './widget-one.container';

describe('WidgetOne.Container', () => {
  it('should create an instance', () => {
    expect(new WidgetOne.Container()).toBeTruthy();
  });
});
