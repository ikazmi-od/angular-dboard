import { WidgetTwo.Container } from './widget-two.container';

describe('WidgetTwo.Container', () => {
  it('should create an instance', () => {
    expect(new WidgetTwo.Container()).toBeTruthy();
  });
});
